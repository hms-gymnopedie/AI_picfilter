# ── AI_picfilter Frontend Environment Variables ───────────────────────────────
# Copy this file to .env.local for local development.
# For production, set these variables in your CI/CD pipeline or Docker build args.

# Base URL of the backend API (no trailing slash)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# Public URL of this frontend app (used for OpenGraph, canonical links, etc.)
NEXT_PUBLIC_APP_URL=https://yourdomain.com
